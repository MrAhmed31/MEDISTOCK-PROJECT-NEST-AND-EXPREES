version: '3.9'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./pharmacy-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always